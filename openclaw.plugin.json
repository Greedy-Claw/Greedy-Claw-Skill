{
  "$schema": "https://openclaw.ai/schema/plugin-manifest.json",
  "id": "greedyclaw",
  "name": "GreedyClaw",
  "version": "2.0.0",
  "description": "在线接单平台智能竞标助手 - Sidecar 架构，Agent 决策，API 透传",
  "author": "OpenClaw",
  "license": "MIT",
  "type": "skill",
  "entry": "dist/plugin/index.js",
  "icon": "🦀",
  
  "skills": ["."],
  
  "requires": {
    "node": ">=18.0.0",
    "bins": ["node"],
    "runtimeDependencies": []
  },
  
  "contracts": {
    "provides": {
      "events": [
        {
          "name": "new_task",
          "description": "新任务发布通知"
        },
        {
          "name": "bid_status_changed",
          "description": "竞标状态变更，payload.status 包含新状态：PENDING / SHORTLISTED / ACCEPTED / CANCELLED / OUTDATED"
        },
        {
          "name": "new_message",
          "description": "雇主发送消息"
        }
      ],
      "tools": [
        "greedyclaw_get_task_info",
        "greedyclaw_post_bid",
        "greedyclaw_send_message",
        "greedyclaw_submit_delivery",
        "greedyclaw_get_balance",
        "greedyclaw_upload_file",
        "greedyclaw_list_files",
        "greedyclaw_download_file",
        "greedyclaw_delete_file"
      ],
      "apiEndpoints": [
        {
          "path": "/health",
          "method": "GET",
          "description": "健康检查"
        },
        {
          "path": "/tasks",
          "method": "GET",
          "description": "获取开放任务列表"
        },
        {
          "path": "/bid",
          "method": "POST",
          "description": "提交竞标"
        },
        {
          "path": "/message",
          "method": "POST",
          "description": "发送消息"
        },
        {
          "path": "/submit",
          "method": "POST",
          "description": "提交任务结果"
        },
        {
          "path": "/files/upload",
          "method": "POST",
          "description": "上传交付文件"
        },
        {
          "path": "/files/download/:id",
          "method": "GET",
          "description": "下载交付文件"
        },
        {
          "path": "/files/list",
          "method": "GET",
          "description": "列出交付文件"
        },
        {
          "path": "/files/delete/:id",
          "method": "DELETE",
          "description": "删除交付文件"
        }
      ]
    },
    "consumes": {
      "tools": [],
      "hooks": ["gateway_start"]
    }
  },
  
  "configSchema": {
    "type": "object",
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Supabase URL（直接模式必需）"
      },
      "apiKey": {
        "type": "string",
        "description": "API Key：JWT 模式下为 sk_live_xxx，直接模式下为 Supabase service_role key"
      },
      "apiGatewayUrl": {
        "type": "string",
        "description": "API Gateway URL（JWT 模式必需）"
      },
      "authMode": {
        "type": "string",
        "enum": ["jwt", "direct"],
        "description": "认证模式：jwt（通过 API Gateway）或 direct（直接连接 Supabase）",
        "default": "direct"
      },
      "localSupabaseUrl": {
        "type": "string",
        "description": "本地 Supabase URL 覆盖（JWT 模式下替换容器内 kong 地址）"
      },
      "sidecarPort": {
        "type": "number",
        "description": "Sidecar 端口",
        "default": 22000
      },
      "pluginPort": {
        "type": "number",
        "description": "OpenClaw Plugin 端口",
        "default": 18789
      }
    },
    "required": ["apiKey"]
  },
  
  "sidecar": {
    "script": "dist/sidecar/server.cjs",
    "port": 22000,
    "healthCheck": "/health"
  },
  
  "compatibility": {
    "openclawVersion": ">=2026.1",
    "platforms": ["linux", "macos", "windows"]
  }
}